name: braindrive-mvp

services:
  node-router:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    user: "${HOST_UID:-1001}:${HOST_GID:-1001}"
    command: ["python", "-u", "services/router_service.py"]
    environment:
      PYTHONPATH: /workspace
      ROUTER_PORT: "8080"
      ROUTER_REGISTRATION_TOKEN: "${ROUTER_REGISTRATION_TOKEN:-braindrive-mvp-dev-token}"
      ROUTER_NODE_TIMEOUT_SEC: "${ROUTER_NODE_TIMEOUT_SEC:-180}"
      BRAINDRIVE_LIBRARY_ROOT: /workspace/data/library
      BRAINDRIVE_RUNTIME_DIR: /workspace/data/runtime/router
      BRAINDRIVE_USER_CONFIG_PATH: /workspace/data/runtime/router/user-config.yaml
      BRAINDRIVE_DEFAULT_PROVIDER: "${BRAINDRIVE_DEFAULT_PROVIDER:-openrouter}"
      BRAINDRIVE_OPENROUTER_API_KEY: "${BRAINDRIVE_OPENROUTER_API_KEY:-}"
      BRAINDRIVE_OPENROUTER_BASE_URL: "${BRAINDRIVE_OPENROUTER_BASE_URL:-https://openrouter.ai/api/v1}"
      BRAINDRIVE_OPENROUTER_DEFAULT_MODEL: "${BRAINDRIVE_OPENROUTER_DEFAULT_MODEL:-}"
      BRAINDRIVE_OLLAMA_BASE_URL: "${BRAINDRIVE_OLLAMA_BASE_URL:-http://host.docker.internal:11434/v1}"
      BRAINDRIVE_OLLAMA_API_KEY: "${BRAINDRIVE_OLLAMA_API_KEY:-}"
      BRAINDRIVE_OLLAMA_DEFAULT_MODEL: "${BRAINDRIVE_OLLAMA_DEFAULT_MODEL:-llama3:8b}"
      BRAINDRIVE_MODEL_TIMEOUT_SEC: "${BRAINDRIVE_MODEL_TIMEOUT_SEC:-180}"
    volumes:
      - ./data/runtime:/workspace/data/runtime
      - ./data/library:/workspace/data/library:ro
    ports:
      - "${BRAINDRIVE_ROUTER_PORT:-9480}:8080"

  intent-router-natural-language:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    user: "${HOST_UID:-1001}:${HOST_GID:-1001}"
    command: ["python", "-u", "services/intent_router_service.py"]
    environment:
      PYTHONPATH: /workspace
      INTENT_ROUTER_PORT: "8081"
      INTENT_ROUTER_ROUTER_BASE_URL: http://node-router:8080
      INTENT_ROUTER_CATALOG_TIMEOUT_SEC: "${INTENT_ROUTER_CATALOG_TIMEOUT_SEC:-5}"
      INTENT_ROUTER_ROUTE_TIMEOUT_SEC: "${INTENT_ROUTER_ROUTE_TIMEOUT_SEC:-240}"
      BRAINDRIVE_ENABLE_TEST_ENDPOINTS: "${BRAINDRIVE_ENABLE_TEST_ENDPOINTS:-false}"
      BRAINDRIVE_RUNTIME_DIR: /workspace/data/runtime/intent-router
      BRAINDRIVE_WORKFLOW_FULL_TRACE: "${BRAINDRIVE_WORKFLOW_FULL_TRACE:-true}"
    volumes:
      - ./data/runtime:/workspace/data/runtime
      - ./data/library:/workspace/data/library:ro
    ports:
      - "${BRAINDRIVE_INTENT_PORT:-9481}:8081"
    depends_on:
      - node-router

  gateway:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    user: "${HOST_UID:-1001}:${HOST_GID:-1001}"
    command: ["python", "-u", "services/gateway_service.py"]
    environment:
      PYTHONPATH: /workspace
      GATEWAY_PORT: "8090"
      GATEWAY_INTENT_ROUTER_BASE_URL: http://intent-router-natural-language:8081
      GATEWAY_ROUTER_BASE_URL: http://node-router:8080
      GATEWAY_HTTP_TIMEOUT_SEC: "${GATEWAY_HTTP_TIMEOUT_SEC:-240}"
      GATEWAY_AUTH_REQUIRED: "${GATEWAY_AUTH_REQUIRED:-true}"
      GATEWAY_ENFORCE_SESSION: "${GATEWAY_ENFORCE_SESSION:-false}"
      GATEWAY_SESSION_TTL_SEC: "${GATEWAY_SESSION_TTL_SEC:-43200}"
      GATEWAY_REFRESH_TTL_SEC: "${GATEWAY_REFRESH_TTL_SEC:-604800}"
      GATEWAY_AUTH_SALT: "${GATEWAY_AUTH_SALT:-braindrive-gateway-salt}"
      GATEWAY_API_KEYS: "${GATEWAY_API_KEYS:-}"
      BRAINDRIVE_RUNTIME_DIR: /workspace/data/runtime/gateway
    volumes:
      - ./data/runtime:/workspace/data/runtime
      - ./data/library:/workspace/data/library:ro
    ports:
      - "${BRAINDRIVE_GATEWAY_PORT:-9482}:8090"
    depends_on:
      - intent-router-natural-language

  node-web-console-tty:
    build:
      context: .
      dockerfile: Dockerfile.webterm
    working_dir: /workspace
    user: "${HOST_UID:-1001}:${HOST_GID:-1001}"
    command: ["/workspace/scripts/tty_webterm_entry.sh"]
    environment:
      PYTHONPATH: /workspace
      BRAINDRIVE_GATEWAY_BASE: "http://gateway:8090"
      BRAINDRIVE_ROUTER_BASE: "http://node-router:8080"
      BRAINDRIVE_INTENT_BASE: "http://intent-router-natural-language:8081"
      BRAINDRIVE_RUNTIME_DIR: "/workspace/data/runtime"
      BRAINDRIVE_LIBRARY_ROOT: "/workspace/data/library"
      BRAINDRIVE_CLI_HISTORY_FILE: "/workspace/data/runtime/state/.cli_history"
      BRAINDRIVE_CLI_TIMEOUT_SEC: "${BRAINDRIVE_CLI_TIMEOUT_SEC:-300}"
      BRAINDRIVE_CLI_COLOR: "${BRAINDRIVE_CLI_COLOR:-auto}"
      BRAINDRIVE_CLI_STREAM_MODEL_CHAT: "${BRAINDRIVE_CLI_STREAM_MODEL_CHAT:-true}"
      BRAINDRIVE_CLI_STREAM_FALLBACK_ONLY: "${BRAINDRIVE_CLI_STREAM_FALLBACK_ONLY:-true}"
      BRAINDRIVE_CLI_STREAM_DIAGNOSTICS: "${BRAINDRIVE_CLI_STREAM_DIAGNOSTICS:-false}"
      TTY_WEBTERM_AUTH_ENABLED: "${TTY_WEBTERM_AUTH_ENABLED:-false}"
      TTY_WEBTERM_AUTH_USER: "${TTY_WEBTERM_AUTH_USER:-dev}"
      TTY_WEBTERM_AUTH_PASSWORD: "${TTY_WEBTERM_AUTH_PASSWORD:-change-me-now}"
      TTY_WEBTERM_LOG_LEVEL: "${TTY_WEBTERM_LOG_LEVEL:-2}"
      TTY_WEBTERM_PORT_INTERNAL: "7681"
      TTY_WEBTERM_THEME_BACKGROUND: "${TTY_WEBTERM_THEME_BACKGROUND:-#000000}"
    volumes:
      - ./data/runtime/tty-webterm:/workspace/data/runtime
      - ./data/library:/workspace/data/library:ro
    ports:
      - "${BRAINDRIVE_WEBCONSOLE_PORT:-9493}:7681"
    depends_on:
      - gateway
      - node-router
      - intent-router-natural-language

  node-runtime-bootstrap:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    user: "${HOST_UID:-1001}:${HOST_GID:-1001}"
    command: ["python", "-u", "services/node_runtime_bootstrap_service.py"]
    environment:
      PYTHONPATH: /workspace
      NODE_KIND: runtime_bootstrap
      NODE_PORT: "8120"
      NODE_ENDPOINT_URL: http://node-runtime-bootstrap:8120/bdp
      ROUTER_REGISTRATION_TOKEN: "${ROUTER_REGISTRATION_TOKEN:-braindrive-mvp-dev-token}"
      ROUTER_REGISTER_URL: http://node-router:8080/router/node/register
      ROUTER_HEARTBEAT_URL: http://node-router:8080/router/node/heartbeat
      ROUTER_DIRECT_ROUTE_TIMEOUT_SEC: "${ROUTER_DIRECT_ROUTE_TIMEOUT_SEC:-180}"
      BRAINDRIVE_LIBRARY_ROOT: /workspace/data/library
      BRAINDRIVE_RUNTIME_DIR: /workspace/data/runtime/runtime-bootstrap
    volumes:
      - ./data/runtime:/workspace/data/runtime
      - ./data/library:/workspace/data/library
    depends_on:
      - node-router

  node-memory-fs:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    user: "${HOST_UID:-1001}:${HOST_GID:-1001}"
    command: ["python", "-u", "services/node_memory_fs_service.py"]
    environment:
      PYTHONPATH: /workspace
      NODE_KIND: memory_fs
      NODE_PORT: "8121"
      NODE_ENDPOINT_URL: http://node-memory-fs:8121/bdp
      ROUTER_REGISTRATION_TOKEN: "${ROUTER_REGISTRATION_TOKEN:-braindrive-mvp-dev-token}"
      ROUTER_REGISTER_URL: http://node-router:8080/router/node/register
      ROUTER_HEARTBEAT_URL: http://node-router:8080/router/node/heartbeat
      ROUTER_DIRECT_ROUTE_TIMEOUT_SEC: "${ROUTER_DIRECT_ROUTE_TIMEOUT_SEC:-180}"
      BRAINDRIVE_LIBRARY_ROOT: /workspace/data/library
      BRAINDRIVE_RUNTIME_DIR: /workspace/data/runtime/memory-fs
    volumes:
      - ./data/runtime:/workspace/data/runtime
      - ./data/library:/workspace/data/library
    depends_on:
      - node-router

  node-session-state:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    user: "${HOST_UID:-1001}:${HOST_GID:-1001}"
    command: ["python", "-u", "services/node_session_state_service.py"]
    environment:
      PYTHONPATH: /workspace
      NODE_KIND: session_state
      NODE_PORT: "8124"
      NODE_ENDPOINT_URL: http://node-session-state:8124/bdp
      ROUTER_REGISTRATION_TOKEN: "${ROUTER_REGISTRATION_TOKEN:-braindrive-mvp-dev-token}"
      ROUTER_REGISTER_URL: http://node-router:8080/router/node/register
      ROUTER_HEARTBEAT_URL: http://node-router:8080/router/node/heartbeat
      ROUTER_DIRECT_ROUTE_TIMEOUT_SEC: "${ROUTER_DIRECT_ROUTE_TIMEOUT_SEC:-180}"
      BRAINDRIVE_LIBRARY_ROOT: /workspace/data/library
      BRAINDRIVE_RUNTIME_DIR: /workspace/data/runtime/session-state
    volumes:
      - ./data/runtime:/workspace/data/runtime
      - ./data/library:/workspace/data/library:ro
    depends_on:
      - node-router

  node-workflow-folder:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    user: "${HOST_UID:-1001}:${HOST_GID:-1001}"
    command: ["python", "-u", "services/node_workflow_folder_service.py"]
    environment:
      PYTHONPATH: /workspace
      NODE_KIND: folder
      NODE_PORT: "8122"
      NODE_ENDPOINT_URL: http://node-workflow-folder:8122/bdp
      ROUTER_REGISTRATION_TOKEN: "${ROUTER_REGISTRATION_TOKEN:-braindrive-mvp-dev-token}"
      ROUTER_REGISTER_URL: http://node-router:8080/router/node/register
      ROUTER_HEARTBEAT_URL: http://node-router:8080/router/node/heartbeat
      ROUTER_DIRECT_ROUTE_TIMEOUT_SEC: "${ROUTER_DIRECT_ROUTE_TIMEOUT_SEC:-180}"
      BRAINDRIVE_LIBRARY_ROOT: /workspace/data/library
      BRAINDRIVE_RUNTIME_DIR: /workspace/data/runtime/workflow-folder
    volumes:
      - ./data/runtime:/workspace/data/runtime
      - ./data/library:/workspace/data/library
    depends_on:
      - node-router

  node-workflow-skill:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    user: "${HOST_UID:-1001}:${HOST_GID:-1001}"
    command: ["python", "-u", "services/node_workflow_skill_service.py"]
    environment:
      PYTHONPATH: /workspace
      NODE_KIND: skill
      NODE_PORT: "8123"
      NODE_ENDPOINT_URL: http://node-workflow-skill:8123/bdp
      ROUTER_REGISTRATION_TOKEN: "${ROUTER_REGISTRATION_TOKEN:-braindrive-mvp-dev-token}"
      ROUTER_REGISTER_URL: http://node-router:8080/router/node/register
      ROUTER_HEARTBEAT_URL: http://node-router:8080/router/node/heartbeat
      ROUTER_DIRECT_ROUTE_TIMEOUT_SEC: "${ROUTER_DIRECT_ROUTE_TIMEOUT_SEC:-180}"
      BRAINDRIVE_LIBRARY_ROOT: /workspace/data/library
      BRAINDRIVE_RUNTIME_DIR: /workspace/data/runtime/workflow-skill
      BRAINDRIVE_USER_CONFIG_PATH: /workspace/data/runtime/workflow-skill/user-config.yaml
      BRAINDRIVE_DEFAULT_PROVIDER: "${BRAINDRIVE_DEFAULT_PROVIDER:-openrouter}"
      BRAINDRIVE_OPENROUTER_DEFAULT_MODEL: "${BRAINDRIVE_OPENROUTER_DEFAULT_MODEL:-}"
      BRAINDRIVE_OLLAMA_DEFAULT_MODEL: "${BRAINDRIVE_OLLAMA_DEFAULT_MODEL:-}"
    volumes:
      - ./data/runtime:/workspace/data/runtime
      - ./data/library:/workspace/data/library:ro
    depends_on:
      - node-router

  node-approval-gate:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    user: "${HOST_UID:-1001}:${HOST_GID:-1001}"
    command: ["python", "-u", "services/node_approval_gate_service.py"]
    environment:
      PYTHONPATH: /workspace
      NODE_KIND: approval_gate
      NODE_PORT: "8126"
      NODE_ENDPOINT_URL: http://node-approval-gate:8126/bdp
      ROUTER_REGISTRATION_TOKEN: "${ROUTER_REGISTRATION_TOKEN:-braindrive-mvp-dev-token}"
      ROUTER_REGISTER_URL: http://node-router:8080/router/node/register
      ROUTER_HEARTBEAT_URL: http://node-router:8080/router/node/heartbeat
      ROUTER_DIRECT_ROUTE_TIMEOUT_SEC: "${ROUTER_DIRECT_ROUTE_TIMEOUT_SEC:-180}"
      BRAINDRIVE_LIBRARY_ROOT: /workspace/data/library
      BRAINDRIVE_RUNTIME_DIR: /workspace/data/runtime/approval-gate
    volumes:
      - ./data/runtime:/workspace/data/runtime
      - ./data/library:/workspace/data/library:ro
    depends_on:
      - node-router

  node-git-ops:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    user: "${HOST_UID:-1001}:${HOST_GID:-1001}"
    command: ["python", "-u", "services/node_git_ops_service.py"]
    environment:
      PYTHONPATH: /workspace
      NODE_KIND: git_ops
      NODE_PORT: "8127"
      NODE_ENDPOINT_URL: http://node-git-ops:8127/bdp
      ROUTER_REGISTRATION_TOKEN: "${ROUTER_REGISTRATION_TOKEN:-braindrive-mvp-dev-token}"
      ROUTER_REGISTER_URL: http://node-router:8080/router/node/register
      ROUTER_HEARTBEAT_URL: http://node-router:8080/router/node/heartbeat
      ROUTER_DIRECT_ROUTE_TIMEOUT_SEC: "${ROUTER_DIRECT_ROUTE_TIMEOUT_SEC:-180}"
      BRAINDRIVE_LIBRARY_ROOT: /workspace/data/library
      BRAINDRIVE_RUNTIME_DIR: /workspace/data/runtime/git-ops
    volumes:
      - ./data/runtime:/workspace/data/runtime
      - ./data/library:/workspace/data/library
    depends_on:
      - node-router

  node-model-openrouter:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    user: "${HOST_UID:-1001}:${HOST_GID:-1001}"
    command: ["python", "-u", "services/node_model_openrouter_service.py"]
    environment:
      PYTHONPATH: /workspace
      NODE_KIND: model_openrouter
      NODE_PORT: "8128"
      NODE_ENDPOINT_URL: http://node-model-openrouter:8128/bdp
      ROUTER_REGISTRATION_TOKEN: "${ROUTER_REGISTRATION_TOKEN:-braindrive-mvp-dev-token}"
      ROUTER_REGISTER_URL: http://node-router:8080/router/node/register
      ROUTER_HEARTBEAT_URL: http://node-router:8080/router/node/heartbeat
      ROUTER_DIRECT_ROUTE_TIMEOUT_SEC: "${ROUTER_DIRECT_ROUTE_TIMEOUT_SEC:-180}"
      BRAINDRIVE_LIBRARY_ROOT: /workspace/data/library
      BRAINDRIVE_RUNTIME_DIR: /workspace/data/runtime/model-openrouter
      BRAINDRIVE_OPENROUTER_API_KEY: "${BRAINDRIVE_OPENROUTER_API_KEY:-}"
      BRAINDRIVE_OPENROUTER_BASE_URL: "${BRAINDRIVE_OPENROUTER_BASE_URL:-https://openrouter.ai/api/v1}"
      BRAINDRIVE_OPENROUTER_DEFAULT_MODEL: "${BRAINDRIVE_OPENROUTER_DEFAULT_MODEL:-}"
      BRAINDRIVE_OPENROUTER_SITE_URL: "${BRAINDRIVE_OPENROUTER_SITE_URL:-}"
      BRAINDRIVE_OPENROUTER_APP_NAME: "${BRAINDRIVE_OPENROUTER_APP_NAME:-BrainDrive-MVP}"
      BRAINDRIVE_MODEL_TIMEOUT_SEC: "${BRAINDRIVE_MODEL_TIMEOUT_SEC:-180}"
    volumes:
      - ./data/runtime:/workspace/data/runtime
      - ./data/library:/workspace/data/library:ro
    depends_on:
      - node-router

  node-model-ollama:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    user: "${HOST_UID:-1001}:${HOST_GID:-1001}"
    command: ["python", "-u", "services/node_model_ollama_service.py"]
    environment:
      PYTHONPATH: /workspace
      NODE_KIND: model_ollama
      NODE_PORT: "8129"
      NODE_ENDPOINT_URL: http://node-model-ollama:8129/bdp
      ROUTER_REGISTRATION_TOKEN: "${ROUTER_REGISTRATION_TOKEN:-braindrive-mvp-dev-token}"
      ROUTER_REGISTER_URL: http://node-router:8080/router/node/register
      ROUTER_HEARTBEAT_URL: http://node-router:8080/router/node/heartbeat
      ROUTER_DIRECT_ROUTE_TIMEOUT_SEC: "${ROUTER_DIRECT_ROUTE_TIMEOUT_SEC:-180}"
      BRAINDRIVE_LIBRARY_ROOT: /workspace/data/library
      BRAINDRIVE_RUNTIME_DIR: /workspace/data/runtime/model-ollama
      BRAINDRIVE_OLLAMA_BASE_URL: "${BRAINDRIVE_OLLAMA_BASE_URL:-http://host.docker.internal:11434/v1}"
      BRAINDRIVE_OLLAMA_API_KEY: "${BRAINDRIVE_OLLAMA_API_KEY:-}"
      BRAINDRIVE_OLLAMA_DEFAULT_MODEL: "${BRAINDRIVE_OLLAMA_DEFAULT_MODEL:-llama3:8b}"
      BRAINDRIVE_MODEL_TIMEOUT_SEC: "${BRAINDRIVE_MODEL_TIMEOUT_SEC:-180}"
    volumes:
      - ./data/runtime:/workspace/data/runtime
      - ./data/library:/workspace/data/library:ro
    depends_on:
      - node-router

  node-chat-general:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    user: "${HOST_UID:-1001}:${HOST_GID:-1001}"
    command: ["python", "-u", "services/node_chat_general_service.py"]
    environment:
      PYTHONPATH: /workspace
      NODE_KIND: chat_general
      NODE_PORT: "8130"
      NODE_ENDPOINT_URL: http://node-chat-general:8130/bdp
      ROUTER_REGISTRATION_TOKEN: "${ROUTER_REGISTRATION_TOKEN:-braindrive-mvp-dev-token}"
      ROUTER_REGISTER_URL: http://node-router:8080/router/node/register
      ROUTER_HEARTBEAT_URL: http://node-router:8080/router/node/heartbeat
      ROUTER_DIRECT_ROUTE_TIMEOUT_SEC: "${ROUTER_DIRECT_ROUTE_TIMEOUT_SEC:-180}"
      BRAINDRIVE_LIBRARY_ROOT: /workspace/data/library
      BRAINDRIVE_RUNTIME_DIR: /workspace/data/runtime/chat-general
    volumes:
      - ./data/runtime:/workspace/data/runtime
      - ./data/library:/workspace/data/library:ro
    depends_on:
      - node-router

  node-web-scrapling:
    build:
      context: .
      dockerfile: Dockerfile.scrapling
    working_dir: /workspace
    user: "${HOST_UID:-1001}:${HOST_GID:-1001}"
    command: ["python", "-u", "services/node_web_scrapling_service.py"]
    environment:
      PYTHONPATH: /workspace
      NODE_KIND: scrapling
      NODE_PORT: "8132"
      NODE_ENDPOINT_URL: http://node-web-scrapling:8132/bdp
      ROUTER_REGISTRATION_TOKEN: "${ROUTER_REGISTRATION_TOKEN:-braindrive-mvp-dev-token}"
      ROUTER_REGISTER_URL: http://node-router:8080/router/node/register
      ROUTER_HEARTBEAT_URL: http://node-router:8080/router/node/heartbeat
      ROUTER_DIRECT_ROUTE_TIMEOUT_SEC: "${ROUTER_DIRECT_ROUTE_TIMEOUT_SEC:-180}"
      BRAINDRIVE_LIBRARY_ROOT: /workspace/data/library
      BRAINDRIVE_RUNTIME_DIR: /workspace/data/runtime/web-scrapling
      BRAINDRIVE_SCRAPLING_ENABLED: "${BRAINDRIVE_SCRAPLING_ENABLED:-true}"
      BRAINDRIVE_SCRAPLING_ALLOW_PRIVATE_NET: "${BRAINDRIVE_SCRAPLING_ALLOW_PRIVATE_NET:-false}"
      BRAINDRIVE_SCRAPLING_ALLOWED_DOMAINS: "${BRAINDRIVE_SCRAPLING_ALLOWED_DOMAINS:-}"
      BRAINDRIVE_SCRAPLING_BLOCKED_DOMAINS: "${BRAINDRIVE_SCRAPLING_BLOCKED_DOMAINS:-}"
      BRAINDRIVE_SCRAPLING_MAX_URLS: "${BRAINDRIVE_SCRAPLING_MAX_URLS:-10}"
      BRAINDRIVE_SCRAPLING_MAX_CONTENT_CHARS: "${BRAINDRIVE_SCRAPLING_MAX_CONTENT_CHARS:-120000}"
      BRAINDRIVE_SCRAPLING_TIMEOUT_SEC: "${BRAINDRIVE_SCRAPLING_TIMEOUT_SEC:-30}"
      BRAINDRIVE_SCRAPLING_BROWSER_TIMEOUT_MS: "${BRAINDRIVE_SCRAPLING_BROWSER_TIMEOUT_MS:-30000}"
      BRAINDRIVE_SCRAPLING_STEALTH_ENABLED: "${BRAINDRIVE_SCRAPLING_STEALTH_ENABLED:-true}"
      BRAINDRIVE_SCRAPLING_DEFAULT_SAVE: "${BRAINDRIVE_SCRAPLING_DEFAULT_SAVE:-true}"
      BRAINDRIVE_SCRAPLING_DEFAULT_SAVE_DIR: "${BRAINDRIVE_SCRAPLING_DEFAULT_SAVE_DIR:-scraping}"
    volumes:
      - ./data/runtime:/workspace/data/runtime
      - ./data/library:/workspace/data/library
    depends_on:
      - node-router

  node-web-console:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    user: "${HOST_UID:-1001}:${HOST_GID:-1001}"
    command: ["python", "-u", "services/node_web_console_service.py"]
    environment:
      PYTHONPATH: /workspace
      NODE_KIND: web_console
      NODE_PORT: "8133"
      NODE_ENDPOINT_URL: http://node-web-console:8133/bdp
      ROUTER_REGISTRATION_TOKEN: "${ROUTER_REGISTRATION_TOKEN:-braindrive-mvp-dev-token}"
      ROUTER_REGISTER_URL: http://node-router:8080/router/node/register
      ROUTER_HEARTBEAT_URL: http://node-router:8080/router/node/heartbeat
      ROUTER_DIRECT_ROUTE_TIMEOUT_SEC: "${ROUTER_DIRECT_ROUTE_TIMEOUT_SEC:-180}"
      BRAINDRIVE_LIBRARY_ROOT: /workspace/data/library
      BRAINDRIVE_RUNTIME_DIR: /workspace/data/runtime/web-console
      WEBTERM_ENABLED: "${WEBTERM_ENABLED:-true}"
      WEBTERM_ALLOWED_ORIGINS: "${WEBTERM_ALLOWED_ORIGINS-}"
      WEBTERM_SESSION_IDLE_TIMEOUT_SEC: "${WEBTERM_SESSION_IDLE_TIMEOUT_SEC:-900}"
      WEBTERM_SESSION_MAX_TIMEOUT_SEC: "${WEBTERM_SESSION_MAX_TIMEOUT_SEC:-3600}"
      WEBTERM_MAX_CONCURRENT_SESSIONS_PER_USER: "${WEBTERM_MAX_CONCURRENT_SESSIONS_PER_USER:-2}"
      WEBTERM_MAX_MESSAGE_BYTES: "${WEBTERM_MAX_MESSAGE_BYTES:-4096}"
      WEBTERM_MAX_EVENTS_PER_MINUTE: "${WEBTERM_MAX_EVENTS_PER_MINUTE:-120}"
      WEBTERM_TARGETS: "${WEBTERM_TARGETS:-node-router,node-memory-fs,node-session-state,node-workflow-folder,node-workflow-skill,node-chat-general}"
      WEBTERM_SSH_GATEWAY_HOST: "${WEBTERM_SSH_GATEWAY_HOST:-ssh-gateway}"
      WEBTERM_SSH_GATEWAY_PORT: "${WEBTERM_SSH_GATEWAY_PORT:-2226}"
      WEBTERM_SSH_TARGET_DEFAULT: "${WEBTERM_SSH_TARGET_DEFAULT:-node-router}"
      WEBTERM_SSH_AUTH_MODE: "${WEBTERM_SSH_AUTH_MODE:-disabled}"
      WEBTERM_SSH_CA_KEY_FILE: "${WEBTERM_SSH_CA_KEY_FILE:-}"
      WEBTERM_SSH_CLIENT_KEY_FILE: "${WEBTERM_SSH_CLIENT_KEY_FILE:-}"
      WEBTERM_SSH_CLIENT_PUB_FILE: "${WEBTERM_SSH_CLIENT_PUB_FILE:-}"
      WEBTERM_SSH_KNOWN_HOSTS_FILE: "${WEBTERM_SSH_KNOWN_HOSTS_FILE:-}"
      WEBTERM_SSH_AUTHORIZED_KEYS_FILE: "${WEBTERM_SSH_AUTHORIZED_KEYS_FILE:-}"
      WEBTERM_SSH_CLIENT_KEY_B64: "${WEBTERM_SSH_CLIENT_KEY_B64:-}"
      WEBTERM_SSH_CLIENT_PUB_B64: "${WEBTERM_SSH_CLIENT_PUB_B64:-}"
      WEBTERM_SSH_AUTHORIZED_KEYS_B64: "${WEBTERM_SSH_AUTHORIZED_KEYS_B64:-}"
      WEBTERM_SSH_EXEC_INTENT: "${WEBTERM_SSH_EXEC_INTENT:-}"
      WEBTERM_GATEWAY_BASE_URL: "${WEBTERM_GATEWAY_BASE_URL:-http://gateway:8090}"
      WEBTERM_INTENT_ROUTER_BASE_URL: "${WEBTERM_INTENT_ROUTER_BASE_URL:-http://intent-router-natural-language:8081}"
      WEBTERM_ROUTER_BASE_URL: "${WEBTERM_ROUTER_BASE_URL:-http://node-router:8080}"
      WEBTERM_HTTP_TIMEOUT_SEC: "${WEBTERM_HTTP_TIMEOUT_SEC:-60}"
    volumes:
      - ./data/runtime:/workspace/data/runtime
      - ./data/library:/workspace/data/library:ro
    depends_on:
      - node-router
      - gateway

  dev-web-terminal:
    profiles: ["dev"]
    build:
      context: .
      dockerfile: Dockerfile.webterm
    working_dir: /workspace
    user: "${HOST_UID:-1001}:${HOST_GID:-1001}"
    command: ["/workspace/scripts/dev_webterm_entry.sh"]
    environment:
      PYTHONPATH: /workspace
      BRAINDRIVE_ROUTER_BASE: "http://node-router:8080"
      BRAINDRIVE_INTENT_BASE: "http://intent-router-natural-language:8081"
      BRAINDRIVE_GATEWAY_BASE: "http://gateway:8090"
      BRAINDRIVE_RUNTIME_DIR: "/workspace/data/runtime"
      BRAINDRIVE_LIBRARY_ROOT: "/workspace/data/library"
      BRAINDRIVE_CLI_HISTORY_FILE: "/workspace/data/runtime/state/.cli_history"
      BRAINDRIVE_CLI_TIMEOUT_SEC: "${BRAINDRIVE_CLI_TIMEOUT_SEC:-300}"
      BRAINDRIVE_CLI_COLOR: "${BRAINDRIVE_CLI_COLOR:-auto}"
      BRAINDRIVE_CLI_STREAM_MODEL_CHAT: "${BRAINDRIVE_CLI_STREAM_MODEL_CHAT:-true}"
      BRAINDRIVE_CLI_STREAM_FALLBACK_ONLY: "${BRAINDRIVE_CLI_STREAM_FALLBACK_ONLY:-true}"
      BRAINDRIVE_CLI_STREAM_DIAGNOSTICS: "${BRAINDRIVE_CLI_STREAM_DIAGNOSTICS:-false}"
      DEV_WEBTERM_AUTH_USER: "${DEV_WEBTERM_AUTH_USER:-dev}"
      DEV_WEBTERM_AUTH_PASSWORD: "${DEV_WEBTERM_AUTH_PASSWORD:-change-me-now}"
      DEV_WEBTERM_LOG_LEVEL: "${DEV_WEBTERM_LOG_LEVEL:-2}"
      DEV_WEBTERM_PORT_INTERNAL: "7681"
    volumes:
      - ./data/runtime/dev-webterm:/workspace/data/runtime
      - ./data/library:/workspace/data/library:ro
    ports:
      - "${BRAINDRIVE_DEV_WEBTERM_BIND_ADDR:-0.0.0.0}:${BRAINDRIVE_DEV_WEBTERM_PORT:-9494}:7681"
    depends_on:
      - node-router
      - intent-router-natural-language

  node-audit-log:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace
    user: "${HOST_UID:-1001}:${HOST_GID:-1001}"
    command: ["python", "-u", "services/node_audit_log_service.py"]
    environment:
      PYTHONPATH: /workspace
      NODE_KIND: audit_log
      NODE_PORT: "8131"
      NODE_ENDPOINT_URL: http://node-audit-log:8131/bdp
      ROUTER_REGISTRATION_TOKEN: "${ROUTER_REGISTRATION_TOKEN:-braindrive-mvp-dev-token}"
      ROUTER_REGISTER_URL: http://node-router:8080/router/node/register
      ROUTER_HEARTBEAT_URL: http://node-router:8080/router/node/heartbeat
      ROUTER_DIRECT_ROUTE_TIMEOUT_SEC: "${ROUTER_DIRECT_ROUTE_TIMEOUT_SEC:-180}"
      BRAINDRIVE_LIBRARY_ROOT: /workspace/data/library
      BRAINDRIVE_RUNTIME_DIR: /workspace/data/runtime/audit-log
    volumes:
      - ./data/runtime:/workspace/data/runtime
      - ./data/library:/workspace/data/library:ro
    depends_on:
      - node-router
